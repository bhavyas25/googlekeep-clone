import React from 'react'

function Header() {
  return (
    <header style={{color: "white", backgroundColor: "#f5ba13",
    margin: "auto -16px",
    padding: "16px 32px",
    boxShadow: "0 0 10px 0 rgba(0, 0, 0, 0.3",
    fontWeight: "400",
    fontSize: "xx-large"}}>
      Keeper.
    </header>
  )
}

export default Header
